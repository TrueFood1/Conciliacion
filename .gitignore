# Credenciales de producción — NUNCA subir (repo es público)
conexion_prod.env
*.env
!*.env.example

# Datos del cruce de inventario (Excel de Daniel + derivados)
inventario_daniel.xlsx
_daniel_raw.csv
cruce_*.csv
reporte_cruce*.md

# Bitácora del proyecto — lleva razones sociales de clientes y datos del caso
BITACORA.md

cache_*.json

# Diccionario de clientes (razones sociales/cédulas)
tf/clientes_dict.json
historial_inventario.jsonl

# Token del proxy de lectura (nunca subir)
.proxy_token

# Extracto bancario BAC (crudo + derivados) — datos financieros, NUNCA subir
*.xls
extracto_limpio.csv
extracto_limpio.js

# Basura de macOS
.DS_Store

# Proxy de lectura: tiene su propio repo git, no es parte de este repo
proxy_lectura/
